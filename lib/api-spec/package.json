{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs'),p='../../lib/api-zod/src/index.ts';let c=fs.readFileSync(p,'utf8');c=c.replace(/export \\\\* from '\\.\\/generated\\/types';?\\n?/g,'');fs.writeFileSync(p,c);\" && pnpm -w run typecheck:libs && node -e \"const fs=require('fs'),crypto=require('crypto');fs.writeFileSync('./.codegen-hash',crypto.createHash('sha256').update(fs.readFileSync('./openapi.yaml')).digest('hex')+'\\n');\" && node -e \"const fs=require('fs'),crypto=require('crypto'),path=require('path');function hashDir(d){const h=crypto.createHash('sha256');if(!fs.existsSync(d))return h.digest('hex');(function walk(dir,base){fs.readdirSync(dir,{withFileTypes:true}).sort(function(a,b){return a.name.localeCompare(b.name);}).forEach(function(e){const full=path.join(dir,e.name),rel=path.join(base,e.name);if(e.isDirectory()){walk(full,rel);}else if(e.isFile()){h.update(rel);h.update(fs.readFileSync(full));}});}(d,''));return h.digest('hex');}const dirs=['../../lib/api-client-react/src/generated','../../lib/api-zod/src/generated'];const h=crypto.createHash('sha256');dirs.forEach(function(d){h.update(hashDir(path.resolve(process.cwd(),d)));});fs.writeFileSync('./.codegen-output-hash',h.digest('hex')+'\\n');\""
  },
  "devDependencies": {
    "orval": "^8.9.1"
  }
}
